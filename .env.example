# Production Configuration for Support Q&A Web Interface
# Copy this file to .env and update the values

# Secret key for session management (REQUIRED for production)
# Generate with: python -c "import secrets; print(secrets.token_hex(32))"
SECRET_KEY=your-secret-key-here-change-this-in-production

# Server Configuration
HOST=0.0.0.0
PORT=8000
THREADS=4

# Security Settings
# Set to True if running behind HTTPS/TLS (recommended for production)
HTTPS_ENABLED=False

# Rate Limiting Storage
# Options: memory:// (default), redis://localhost:6379
RATE_LIMIT_STORAGE=memory://

# Note: For production deployment, consider:
# 1. Generate a strong SECRET_KEY
# 2. Enable HTTPS_ENABLED=True with proper SSL/TLS certificates
# 3. Use Redis for rate limiting if running multiple instances
# 4. Run behind a reverse proxy (nginx, Apache) for better performance
# 5. Set up proper firewall rules
